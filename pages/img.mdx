# img
generate image using the `easy-pil` library
```py
img = await img.load_image_async(user.avatar_url)
editor = img.Editor(img).circle_image()

add_attachment(img, filename="pfp.png")
```
see detailed documentation [here](https://easy-pil.readthedocs.io/en/latest/)